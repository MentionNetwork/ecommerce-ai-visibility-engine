{
  "name": "ecommerce-ai-visibility-engine",
  "private": true,
  "description": "Open-source ecommerce AI visibility engine — see how AI sells your products, ranked against the retailers it recommends instead.",
  "license": "FSL-1.1-ALv2",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  }
}
