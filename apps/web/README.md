# apps/web — self-host report viewer (placeholder)
Next.js app wrapping @mention-network/report-ui for self-hosters. NOT the mention.network cloud webapp — that lives in the private `cloud` repo and consumes the same packages via npm.
